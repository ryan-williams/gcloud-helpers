
alias dcc="gcloud dataproc clusters create"
alias dcd="gcloud dataproc clusters delete"
alias dcl="gcloud dataproc clusters list"
alias dcs="gcloud dataproc clusters describe"
alias dcu="gcloud dataproc clusters update"

# The next line updates PATH for the Google Cloud SDK.
if [ -f /Users/ryan/google-cloud-sdk/path.bash.inc ]; then
  source '/Users/ryan/google-cloud-sdk/path.bash.inc'
fi

# The next line enables shell command completion for gcloud.
if [ -f /Users/ryan/google-cloud-sdk/completion.bash.inc ]; then
  source '/Users/ryan/google-cloud-sdk/completion.bash.inc'
fi

alias gcppi="gcloud projects get-iam-policy"
alias gcpcgc="gcloud container clusters get-credentials"
alias gcplr="gcloud beta iam roles list"

# gcloud functions deploy <name> --runtime python37 --trigger-resource ${TRIGGER_BUCKET} --trigger-event google.storage.object.finalize

# gcloud projects add-iam-policy-binding $PROJECT --member serviceAccount:$SVCACCT --role roles/$ROLE

export CLOUDSDK_PYTHON="$(which -a python2 | grep -v pyenv | head -n 1)"
if [ "$CLOUDSDK_PYTHON" = "" ]; then
  echo "No python2 found" >&2
fi
